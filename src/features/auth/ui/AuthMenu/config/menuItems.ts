import { IMenuItems } from '../interface/IMenuItems';

export const menuItems: IMenuItems = {
  entrance: 'entrance',
  registration: 'registration',
};

export const DEFAULT_MENU_ITEM = menuItems.entrance;
